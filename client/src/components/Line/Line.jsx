import React from 'react';

import SubLine from './styles';

const Line = () => {
  const lStyle = {
    marginTop: '16px',
    marginBottom: '16px',
  };

  return (
    <div style={lStyle}>
      <SubLine />
    </div>
  );
};

export default Line;
