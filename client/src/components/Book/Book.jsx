import React from 'react';

import { BookBtn, Container } from '../styles';

const Book = () => {
  return (
    <Container>
      <BookBtn>
        Request to Book
      </BookBtn>
    </Container>
  );
};

export default Book;
