import React from 'react';

import { BookBtn, Container } from '../styles';

const Book = () => (
  <Container>
    <BookBtn>
      Request to Book
    </BookBtn>
  </Container>
);

export default Book;
